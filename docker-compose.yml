services:
  techpulse:
    build: .
    image: techpulse:latest
    env_file: .env
    init: true
    restart: unless-stopped
    environment:
      DB_PATH: /app/data/techpulse.db
      TZ: ${TZ:-Europe/Paris}
    volumes:
      - techpulse-data:/app/data

volumes:
  techpulse-data:
